import React from 'react';
import NavPortfolio from '../../components/NavPortfolio';
import Template from '../../components/Template';

export default function Portfolio() {
  return (
    <Template nav={<NavPortfolio />}>
      <h5>portfolio</h5>
      <div>

      </div>
    </Template>
  )
}